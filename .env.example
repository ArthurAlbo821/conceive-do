# ====================================
# CONCEIVE-DO - Environment Variables
# ====================================
#
# This file contains all required environment variables for the application.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit .env files with real credentials to git!
# The .env file is already in .gitignore for your protection.
#
# ====================================

# ----------------
# SUPABASE CONFIGURATION
# ----------------
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
#
# Your Supabase project URL (required for both frontend and backend)
VITE_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"

# Supabase publishable/anon key (safe to expose in frontend)
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-key-here"

# Supabase project ID (just the ID part, e.g., "abcdefghijklmnop")
VITE_SUPABASE_PROJECT_ID="YOUR_PROJECT_ID"

# ⚠️ BACKEND ONLY - Never expose in frontend!
# Service role key (full admin access - use only in Edge Functions)
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key-here"

# Supabase anon key (for backend Edge Functions)
SUPABASE_ANON_KEY="your-supabase-anon-key-here"

# Supabase URL (for backend Edge Functions)
SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"


# ----------------
# EVOLUTION API (WhatsApp Integration)
# ----------------
# Get these from your Evolution API instance
# Documentation: https://doc.evolution-api.com/
#
# Evolution API base URL (without trailing slash)
EVOLUTION_API_BASE_URL="https://your-evolution-api-domain.com"

# Evolution API key for authentication
EVOLUTION_API_KEY="your-evolution-api-key-here"

# Evolution API global key (if using global authentication)
EVOLUTION_API_GLOBAL_KEY="your-evolution-global-api-key-here"


# ----------------
# OPENAI API (AI Auto-Reply)
# ----------------
# Get your API key from: https://platform.openai.com/api-keys
#
# OpenAI API key for AI-powered auto-reply functionality
OPENAI_API_KEY="sk-your-openai-api-key-here"


# ----------------
# WEBHOOK SECURITY
# ----------------
# Generate a strong random secret for HMAC signature validation
# Use: openssl rand -hex 32
#
# Webhook secret for signature validation (HMAC)
WEBHOOK_SECRET="your-webhook-secret-here-minimum-32-characters"


# ----------------
# DUCKLING API (Optional - Time/Date Parsing)
# ----------------
# If you're self-hosting Duckling for temporal parsing
# Leave empty to skip Duckling integration
#
# Duckling API URL (optional)
DUCKLING_API_URL="https://your-duckling-instance.com/parse"


# ----------------
# DATABASE (Direct PostgreSQL Access)
# ----------------
# ⚠️ ONLY for migrations or direct DB access - NOT used in production app
# Get from: Supabase Dashboard → Settings → Database → Connection String
#
# Full PostgreSQL connection string (use cautiously!)
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_ID.supabase.co:5432/postgres"


# ----------------
# DEVELOPMENT SETTINGS
# ----------------
# Node environment (development, production, test)
NODE_ENV="development"

# Enable debug logging (true/false)
DEBUG="false"


# =====================================
# DEPLOYMENT CHECKLIST
# =====================================
#
# Before deploying to production (Vercel/Netlify/etc.):
#
# ✅ All VITE_* variables configured in build settings
# ✅ SUPABASE_SERVICE_ROLE_KEY added to deployment secrets (never in frontend!)
# ✅ OPENAI_API_KEY added to deployment secrets
# ✅ EVOLUTION_API_KEY added to deployment secrets
# ✅ WEBHOOK_SECRET generated and configured
# ✅ Row Level Security (RLS) enabled on all Supabase tables
# ✅ Supabase Edge Functions secrets configured via: supabase secrets set
# ✅ Production URLs updated (no localhost references)
# ✅ HTTPS enforced for all webhooks
# ✅ Rate limiting configured for Edge Functions
# ✅ CORS properly configured in Supabase
#
# =====================================

# ----------------
# SUPABASE EDGE FUNCTIONS SECRETS
# ----------------
# These should be set via Supabase CLI, not in .env:
#
# supabase secrets set SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
# supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# supabase secrets set SUPABASE_ANON_KEY=your-anon-key
# supabase secrets set EVOLUTION_API_KEY=your-evolution-key
# supabase secrets set EVOLUTION_API_BASE_URL=https://your-evolution-api.com
# supabase secrets set OPENAI_API_KEY=sk-your-openai-key
# supabase secrets set WEBHOOK_SECRET=your-webhook-secret
# supabase secrets set DUCKLING_API_URL=https://your-duckling-instance.com/parse
#
# List all secrets: supabase secrets list
# Unset a secret: supabase secrets unset SECRET_NAME
#
# =====================================
