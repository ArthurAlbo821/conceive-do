{
  "tasks": {
    "test": "deno test --allow-env --allow-net tests/",
    "test:watch": "deno test --allow-env --allow-net --watch tests/",
    "test:coverage": "deno test --allow-env --allow-net --coverage=coverage tests/ && deno coverage coverage"
  }
}
